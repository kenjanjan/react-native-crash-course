import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  <SafeAreaView>
    <Text>Chat</Text>
  </SafeAreaView>;
};

export default Chat;
