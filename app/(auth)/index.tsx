import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
    <SafeAreaView>
      <Text>Home</Text>
    </SafeAreaView>;
};

export default Home;